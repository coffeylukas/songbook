# Backlog — retired

Task tracking moved to **GitHub Issues + the Song Book App Project board**, migrated by
[`scripts/sync_backlog_to_github.py`](../scripts/sync_backlog_to_github.py). This file is kept at
this path only so existing links to it don't break — it's no longer read or written by the
`/next-task` workflow.

- All open work: `gh issue list --label type:autonomous --state open` (or the Project board).
- How the workflow actually runs: [MULTI_AGENT_WORKFLOW.md](MULTI_AGENT_WORKFLOW.md).
- The original task content (descriptions, Definitions of Done, dependencies) lives in git
  history for this file, and was carried into each GitHub issue's body during migration.
